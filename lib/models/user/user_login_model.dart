class UserLoginModel {
  String? email;
  String? password;

  UserLoginModel({
    this.email,
    this.password,
  });

  Map<String, dynamic> toJson() {
    return {
      'email': email,
      'password': password,
    };
  }
}
